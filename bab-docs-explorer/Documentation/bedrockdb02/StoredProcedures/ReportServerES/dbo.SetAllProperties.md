# dbo.SetAllProperties

**Database:** ReportServerES  
**Server:** bedrockdb02  

## Architecture Diagram

```mermaid
flowchart LR
    SP["dbo.SetAllProperties"]
    dbo_Catalog(["dbo.Catalog"]) --> SP
    dbo_GetUserID(["dbo.GetUserID"]) --> SP
    dbo_TempCatalog(["dbo.TempCatalog"]) --> SP
```

## Table Dependencies

| Referenced Table |
|---|
| dbo.Catalog |
| dbo.GetUserID |
| dbo.TempCatalog |

## Stored Procedure Code

```sql

```

