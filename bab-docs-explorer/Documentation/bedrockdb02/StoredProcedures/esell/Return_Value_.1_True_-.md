# Return Value:.1 (True) -

**Database:** esell  
**Server:** bedrockdb02  

## Architecture Diagram

```mermaid
flowchart LR
    SP["Return Value:.1 (True) -"]
    SP --> NoRefs(["No dependencies detected"])
```

## Table Dependencies

_No table references detected._

## Stored Procedure Code

```sql
if 2nd parameter represents a subset (but NOT equal set)
```

