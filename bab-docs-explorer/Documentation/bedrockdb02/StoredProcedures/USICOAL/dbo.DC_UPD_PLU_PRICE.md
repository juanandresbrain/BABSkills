# dbo.DC_UPD_PLU_PRICE

**Database:** USICOAL  
**Server:** bedrockdb02  

## Architecture Diagram

```mermaid
flowchart LR
    SP["dbo.DC_UPD_PLU_PRICE"]
    dbo_ITEM(["dbo.ITEM"]) --> SP
    dbo_PLU_PRICE(["dbo.PLU_PRICE"]) --> SP
    dbo_STORE_ITEM(["dbo.STORE_ITEM"]) --> SP
```

## Table Dependencies

| Referenced Table |
|---|
| dbo.ITEM |
| dbo.PLU_PRICE |
| dbo.STORE_ITEM |

## Stored Procedure Code

```sql

```

