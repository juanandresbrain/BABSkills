# WHERE.b.style_id  BETWEEN @curr_style_id AND @curr_style_id_2

**Database:** ma_01  
**Server:** bedrockdb02  

## Architecture Diagram

```mermaid
flowchart LR
    SP["WHERE.b.style_id  BETWEEN @curr_style_id AND @curr_style_id_2"]
    SP --> NoRefs(["No dependencies detected"])
```

## Table Dependencies

_No table references detected._

## Stored Procedure Code

```sql

```

