# AND.b.calendar_code BETWEEN a.merch_year_wk / 100  AND @last_year

**Database:** ma_01  
**Server:** bedrockdb02  

## Architecture Diagram

```mermaid
flowchart LR
    SP["AND.b.calendar_code BETWEEN a.merch_year_wk / 100  AND @last_year"]
    SP --> NoRefs(["No dependencies detected"])
```

## Table Dependencies

_No table references detected._

## Stored Procedure Code

```sql

```

