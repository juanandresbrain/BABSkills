# WHERE.a.style_id BETWEEN @from_style_id AND @to_style_id

**Database:** ma_01  
**Server:** bedrockdb02  

## Architecture Diagram

```mermaid
flowchart LR
    SP["WHERE.a.style_id BETWEEN @from_style_id AND @to_style_id"]
    SP --> NoRefs(["No dependencies detected"])
```

## Table Dependencies

_No table references detected._

## Stored Procedure Code

```sql

```

