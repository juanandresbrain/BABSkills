# POLocationReportV3

**Workspace:** BI-Bearhouse  
**Dataset ID:** d3067db8-5859-4ed0-ad29-d25a14a38359  

## Tables

| Table | Columns | Measures | Hidden |
|---|---|---|---|
| Header | 33 | 0 |  |
| Detail | 19 | 0 |  |

## Measures

_No measures detected._

## Power Query Source (per table)

### Header

```sql
let
    Source = Sql.Database("4db76rlxaxcuvmuh5kw37wbnqq-m2o53thjetderkgqw4nc6a676e.datawarehouse.fabric.microsoft.com", "LH_D365", [Query="with purchline_dedup as#(lf)(#(lf)    select#(lf)          p.dataareaid#(lf)        , p.purchid#(lf)        , p.itemid#(lf)        , p.deliverypostaladdress#(lf)        , p.inventtransid#(lf)        , p.vendaccount#(lf)        , p.purchstatus#(lf)        , p.babaptospolinedistronum#(lf)        , p.babaptospolinedistrolinenum#(lf)        , p.linenumber#(lf)        , p.name#(lf)        , p.purchqty#(lf)        , p.qtyordered#(lf)        , p.currencycode#(lf)        , row_number() over#(lf)          (#(lf)              partition by#(lf)                    p.dataareaid#(lf)                  , p.purchid#(lf)                  , p.itemid#(lf)                  , p.deliverypostaladdress#(lf)                  , p.inventtransid#(lf)              order by p.linenumber#(lf)          ) as [rn]#(lf)    from dbo.purchline p#(lf)),#(lf)load_status_enum as#(lf)(#(lf)    select#(lf)          e.value#(lf)        , min(e.membername) as [membername]#(lf)    from dbo.retailenumvaluetable e#(lf)    where e.enumname = 'WHSLoadStatus'#(lf)    group by e.value#(lf)),#(lf)shipment_status_enum as#(lf)(#(lf)    select#(lf)          e.value#(lf)        , min(e.membername) as [membername]#(lf)    from dbo.retailenumvaluetable e#(lf)    where e.enumname = 'WHSShipmentStatus'#(lf)    group by e.value#(lf))#(lf)select#(lf)      concat(#(lf)            s.dataareaid, '|',#(lf)            s.shipmentid, '|',#(lf)            s.loadid, '|',#(lf)            ll.recid, '|',#(lf)            ll.itemid#(lf)        ) as [shipmentlinekey]#(lf)    , s.dataareaid#(lf)    , s.deliveryname#(lf)    , s.shipmentid#(lf)    , s.shipmentstatus#(lf)    , isnull(eb.membername, 'Open') as [shipstatusname]#(lf)    , s.customerref as [tpmshipmentid]#(lf)    , s.loadid#(lf)    , l.loadstatus#(lf)    , isnull(ea.membername, 'Open') as [loadstatusname]#(lf)    , l.accountnum as [loadaccountnum]#(lf)    , l.custvendref as [loadcustvendref]#(lf)    , l.destinationname as [loaddestinationname]#(lf)    , l.inventlocationid as [loadinventlocationid]#(lf)    , l.inventsiteid as [loadinventsiteid]#(lf)    , l.originname as [loadoriginname]#(lf)    , p.vendaccount as [povendaccount]#(lf)    , p.purchstatus#(lf)    , ll.ordernum as [loadlineordernum]#(lf)    , p.babaptospolinedistronum#(lf)    , p.babaptospolinedistrolinenum#(lf)    , p.linenumber as [polinenumber]#(lf)    , ll.itemid#(lf)    , p.name#(lf)    , left(p.name, len(p.name) / 2) as [halfname]#(lf)    , p.purchqty#(lf)    , p.qtyordered as [poqtyordered]#(lf)    , p.currencycode#(lf)    , ll.qty as [loadlineqty]#(lf)    , ll.workcreatedqty as [loadlineworkcreatedqty]#(lf)    , ll.uom as [loadlineuom]#(lf)    , ll.recid#(lf)from dbo.whsshipmenttable s#(lf)join dbo.whsloadtable l#(lf)    on s.loadid = l.loadid#(lf)   and s.dataareaid = l.dataareaid#(lf)join dbo.whsloadline ll#(lf)    on s.dataareaid = ll.dataareaid#(lf)   and l.loadid = ll.loadid#(lf)   and s.shipmentid = ll.shipmentid#(lf)left join purchline_dedup p#(lf)    on s.dataareaid = p.dataareaid#(lf)   and ll.ordernum = p.purchid#(lf)   and ll.itemid = p.itemid#(lf)   and l.destinationpostaladdress = p.deliverypostaladdress#(lf)   and ll.inventtransid = p.inventtransid#(lf)   and p.[rn] = 1#(lf)left join load_status_enum ea#(lf)    on l.loadstatus = ea.value#(lf)left join shipment_status_enum eb#(lf)    on s.shipmentstatus = eb.value#(lf)where s.loaddirection = 1#(lf)  and s.deliveryname = 'ASN'#(lf)  and s.shipconfirmutcdatetime > dateadd(month, -3, getdate());#(lf)"]),
    #"Changed Type" = Table.TransformColumnTypes(Source,{{"recid", type text}, {"purchqty", Int64.Type}, {"poqtyordered", Int64.Type}, {"loadlineqty", Int64.Type}, {"loadlineworkcreatedqty", Int64.Type}})
in
    #"Changed Type"
```

### Detail

```sql
let
    Source = Sql.Database("4db76rlxaxcuvmuh5kw37wbnqq-m2o53thjetderkgqw4nc6a676e.datawarehouse.fabric.microsoft.com", "LH_D365", [Query="with base_lines as#(lf)(#(lf)    select#(lf)          concat(#(lf)                s.dataareaid, '|',#(lf)                s.shipmentid, '|',#(lf)                s.loadid, '|',#(lf)                ll.recid, '|',#(lf)                ll.itemid#(lf)            ) as [shipmentlinekey]#(lf)        , s.dataareaid#(lf)        , s.shipmentid#(lf)        , s.loadid#(lf)        , ll.recid#(lf)        , ll.itemid#(lf)    from dbo.whsshipmenttable s#(lf)    join dbo.whsloadtable l#(lf)        on s.loadid = l.loadid#(lf)       and s.dataareaid = l.dataareaid#(lf)    join dbo.whsloadline ll#(lf)        on s.dataareaid = ll.dataareaid#(lf)       and l.loadid = ll.loadid#(lf)       and s.shipmentid = ll.shipmentid#(lf)    left join dbo.purchline p#(lf)        on s.dataareaid = p.dataareaid#(lf)       and ll.ordernum = p.purchid#(lf)       and ll.itemid = p.itemid#(lf)       and l.destinationpostaladdress = p.deliverypostaladdress#(lf)       and ll.inventtransid = p.inventtransid#(lf)    where s.loaddirection = 1#(lf)      and s.deliveryname = 'ASN'#(lf)      and s.shipconfirmutcdatetime > dateadd(month, -3, getdate())#(lf)),#(lf)asn_agg as#(lf)(#(lf)    select#(lf)          a.dataareaid#(lf)        , a.shipmentid#(lf)        , a.itemid#(lf)        , a.loadlinerefrecid#(lf)        , a.module#(lf)        , a.licenseplateid#(lf)        , a.uom#(lf)        , sum(a.qty) as [lpqty]#(lf)    from dbo.whsasnitem a#(lf)    join base_lines b#(lf)        on a.dataareaid = b.dataareaid#(lf)       and a.shipmentid = b.shipmentid#(lf)       and a.itemid = b.itemid#(lf)       and a.loadlinerefrecid = b.recid#(lf)    group by#(lf)          a.dataareaid#(lf)        , a.shipmentid#(lf)        , a.itemid#(lf)        , a.loadlinerefrecid#(lf)        , a.module#(lf)        , a.licenseplateid#(lf)        , a.uom#(lf)),#(lf)uom_structure as#(lf)(#(lf)    select distinct#(lf)          us.loadid#(lf)        , us.module#(lf)        , us.licenseplateid#(lf)        , us.shipmentid#(lf)        , us.createdworkid#(lf)    from dbo.whsuomstructure us#(lf)),#(lf)invent_keys as#(lf)(#(lf)    select distinct#(lf)          a.dataareaid#(lf)        , a.itemid#(lf)        , a.licenseplateid#(lf)    from asn_agg a#(lf)),#(lf)inventsum_agg as#(lf)(#(lf)    select#(lf)          i.dataareaid#(lf)        , i.itemid#(lf)        , i.licenseplateid#(lf)        , i.inventlocationid#(lf)        , i.inventstatusid#(lf)        , i.wmslocationid#(lf)        , sum(i.physicalinvent) as [physicalinventory]#(lf)        , sum(i.reservphysical) as [physicalreserved]#(lf)        , sum(i.availphysical) as [availablephysical]#(lf)        , sum(i.onorder) as [onorder]#(lf)        , sum(i.availordered) as [totalavailable]#(lf)    from dbo.inventsum i#(lf)    join invent_keys k#(lf)        on i.dataareaid = k.dataareaid#(lf)       and i.itemid = k.itemid#(lf)       and i.licenseplateid = k.licenseplateid#(lf)    group by#(lf)          i.dataareaid#(lf)        , i.itemid#(lf)        , i.licenseplateid#(lf)        , i.inventlocationid#(lf)        , i.inventstatusid#(lf)        , i.wmslocationid#(lf)#(tab)HAVING sum(i.physicalinvent) <> 0#(lf)#(tab)#(tab)OR sum(i.reservphysical) <> 0#(lf)#(tab)#(tab)OR sum(i.availphysical) <> 0#(lf)#(tab)#(tab)OR sum(i.onorder) <> 0#(lf)#(tab)#(tab)OR sum(i.availordered) <> 0#(lf))#(lf)select#(lf)      b.[shipmentlinekey]#(lf)    , b.dataareaid#(lf)    , b.shipmentid#(lf)    , b.loadid#(lf)    , b.recid#(lf)    , b.itemid#(lf)    , us.createdworkid as [structurecreatedworkid]#(lf)    , a.licenseplateid as [licenseplateid]#(lf)    , a.[lpqty]#(lf)    , a.uom as [asnuom]#(lf)    , i.inventlocationid as [suminventlocationid]#(lf)    , i.inventstatusid#(lf)    , i.wmslocationid#(lf)    , i.[physicalinventory]#(lf)    , i.[physicalreserved]#(lf)    , i.[availablephysical]#(lf)    , i.[onorder]#(lf)    , i.[totalavailable]#(lf)from base_lines b#(lf)join asn_agg a#(lf)    on b.dataareaid = a.dataareaid#(lf)   and b.shipmentid = a.shipmentid#(lf)   and b.itemid = a.itemid#(lf)   and b.recid = a.loadlinerefrecid#(lf)join uom_structure us#(lf)    on b.loadid = us.loadid#(lf)   and a.module = us.module#(lf)   and a.licenseplateid = us.licenseplateid#(lf)   and b.shipmentid = us.shipmentid#(lf)join inventsum_agg i#(lf)    on a.dataareaid = i.dataareaid#(lf)   and a.itemid = i.itemid#(lf)   and a.licenseplateid = i.licenseplateid;"]),
    #"Changed Type" = Table.TransformColumnTypes(Source,{{"recid", type text}, {"lpqty", Int64.Type}, {"physicalinventory", Int64.Type}, {"physicalreserved", Int64.Type}, {"availablephysical", Int64.Type}, {"onorder", Int64.Type}, {"totalavailable", Int64.Type}})
in
    #"Changed Type"
```

## Data Source Cross-References

| Server | Database | Linked SQL Documentation |
|---|---|---|
| 4db76rlxaxcuvmuh5kw37wbnqq-m2o53thjetderkgqw4nc6a676e.datawarehouse.fabric.microsoft.com | LH_D365 | _(not found in SQL documentation)_ |
